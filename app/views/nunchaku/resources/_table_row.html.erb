<tr id="<%= resource_class.name.underscore %>_<%= resource.id %>" class="<%= resource.tints %>">
	<% column_names.each do |column| %>
	  <td class=" <%= cell_classes(column) %>" resource-link = "<%= resource_cell_link(resource) %>">
			<%= resource.table_cell(column) %>
	  </td>
	<% end %>
</tr>
