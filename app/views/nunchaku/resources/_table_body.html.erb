<tbody
  <% if nests.any? %>
    chain="<%= nested_resource_path nests %>"
  <% end %>
>
  <%=
    render :partial => 'table_row.html.erb',
           :collection => collection,
           :as => :resource,
           :locals => {
             :resource_class => resource_class,
             :column_names => column_names
           }
  %>
</tbody>
